require 'spec_helper'

describe NasneChecker do
  it 'has a version number' do
    expect(NasneChecker::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
